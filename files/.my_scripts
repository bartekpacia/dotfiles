#!/bin/bash

if [ -d ~/dotfiles/idea ]; then
  for f in $(ls ~/dotfiles/idea); do
    alias $f=~/dotfiles/idea/$f
  done
fi

alias calc=~/calc

imagesize() {
  source ~/imagesize $1
}

resize() {
  source ~/resize $1
}

echo "Your personal scripts have been loaded :)"